# Copy to infra/.env and fill in real values for anything beyond local dev.
# infra/.env is gitignored; never commit real credentials.

POSTGRES_USER=admin
POSTGRES_PASSWORD=local_dev_only_change_me
POSTGRES_DB=argentum_hft
